库存的物品定义核心在于组合优于继承，以及利用GameplayTagContainer动态填充实例字段，库存定义只有名称和ULyraInventoryItemFragment数组，该Fragment用于扩充组件，比如为其添加UI图像，为其添加对应的武器DA等，而不是用继承
创建WeaponDefinition等，而Instance舍弃了物品原有的实例字段，比如耐久等，转而用TagContainer计数，本质上就是一个从标签映射到整型值int的哈希表，好处同样是为了避免继承，至于InventoryManagerComponent的添加物品与移除物品
涉及到网络同步，而Lyra的网络同步过于复杂，这里不再提及
